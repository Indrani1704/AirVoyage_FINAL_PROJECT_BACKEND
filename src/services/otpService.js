// const admin = require("../config/firebase");

// exports.verifyOTP = async (idToken) => {
//   try {
//     const decoded = await admin.auth().verifyIdToken(idToken);
//     return decoded;
//   } catch (err) {
//     throw new Error("Invalid OTP");
  
// };